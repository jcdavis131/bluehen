---
description: core-api, worker, asn-engine, research scripts
globs: services/**/*.py,packages/**/*.py,scripts/**/*.py
alwaysApply: false
---

# Python platform

| Path | Spec |
|---|---|
| `services/core-api` | 0004 |
| `services/worker` | 0009 |
| `packages/asn-engine` | 0003 |
| `packages/eval-harness` | 0008 |

## Conventions

- `uv run` from repo root; tests: `uv run pytest services/core-api/tests packages/asn-engine/tests -q`
- Tenant isolation: RLS via `app.workspace_id` — admin session for cross-tenant writes
- ML recipe changes: eval gates + `EVIDENCE.md` row before narrative claims

## Local stack (when unblocked)

```bash
pnpm dev:stack && pnpm db:migrate && pnpm bootstrap:orgs
pnpm dev:api && pnpm dev:worker
```

Claim INF-*, RAG-*, RT-*, SRV-* from work queue. Mark done via `pick_task.py`.
